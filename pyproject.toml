[project]
name = "adn-to-protein"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "biopython>=1.88",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
